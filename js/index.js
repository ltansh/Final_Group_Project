ScrollReveal({ reset: true }).reveal(".card:nth-child(1)", {
    delay: 300,
  });
  ScrollReveal({ reset: true }).reveal(".card:nth-child(2)", {
    delay: 550,
  });
  ScrollReveal({ reset: true }).reveal(".card:nth-child(3)", {
    delay: 800,
  });

  // Download App Scroll Reveal
  ScrollReveal({ reset: true }).reveal(".quoteText, .appStore", {
    delay: 300,
  });
  ScrollReveal({ reset: true }).reveal(".appIconCtn", {
    delay: 550,
  });

  // How it works Scroll Reveal
  ScrollReveal({ reset: true }).reveal(".appScreen:nth-child(1)", {
    delay: 300,
  });
  ScrollReveal({ reset: true }).reveal(".appScreen:nth-child(2)", {
    delay: 550,
  });
  ScrollReveal({ reset: true }).reveal(".appScreen:nth-child(3)", {
    delay: 800,
  });

  // Influencers Scroll Reveal
  ScrollReveal({ reset: true }).reveal(".infCard:nth-child(1)", {
    delay: 300,
  });
  ScrollReveal({ reset: true }).reveal(".infCard:nth-child(2)", {
    delay: 550,
  });
  ScrollReveal({ reset: true }).reveal(".infCard:nth-child(3)", {
    delay: 800,
  });

  // Events Scroll Reveal
  ScrollReveal({ reset: true }).reveal("#eventCard1", {
    delay: 300,
  });
  ScrollReveal({ reset: true }).reveal("#eventCard2", {
    delay: 550,
  });
  ScrollReveal({ reset: true }).reveal("#eventCard3", {
    delay: 800,
  });
  ScrollReveal({ reset: true }).reveal("#eventCard4", {
    delay: 1050,
  });